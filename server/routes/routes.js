const index = require('./index');
const users = require('./users');
const alertRouter = require('./users2');

// 路由
// const routes = function (app) {
//     app.use('/', index);
//     app.use('/users', users);
//     app.use('/users2', alertRouter);
// };

module.exports = routes;